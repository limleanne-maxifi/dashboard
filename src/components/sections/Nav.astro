---
interface Props {
  currentPage?: string;
}

const { currentPage = '/' } = Astro.props;

const links = [
  { label: 'Executive',  href: '/executive' },
  { label: 'Events',     href: '/conference-aeo' },
  { label: 'Enterprise', href: '/work' },
  { label: 'Why AEO',    href: '/aeo' },
  { label: 'About',      href: '/about' },
];
---

<header class="site-nav">
  <div class="nav-inner">
  <a href="/" class="logo" aria-label="Maxifi Digital home">
    <img
      src="/logos/maxifi-logo-black.png"
      alt="Maxifi Digital"
      height="32"
      style="height:32px;width:auto;display:block;"
    />
  </a>

  <nav aria-label="Primary">
    <ul class="nav-links" role="list">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class:list={['nav-link', { 'nav-link--active': currentPage === link.href }]}
            aria-current={currentPage === link.href ? 'page' : undefined}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>

  <div class="nav-end">
    <a href="https://checkyourvisibility.maxifidigital.com/" target="_blank" rel="noopener noreferrer" class="nav-cta">Check my visibility →</a>
    <button
      class="hamburger"
      id="nav-hamburger"
      aria-label="Open navigation"
      aria-expanded="false"
      aria-controls="nav-mobile"
    >
      <span></span><span></span><span></span>
    </button>
  </div>
  </div>
</header>

<div class="nav-mobile" id="nav-mobile" aria-hidden="true">
  <nav aria-label="Mobile primary">
    <ul role="list">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class:list={['nav-mobile-link', { 'nav-link--active': currentPage === link.href }]}
            aria-current={currentPage === link.href ? 'page' : undefined}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
    <a href="https://checkyourvisibility.maxifidigital.com/" target="_blank" rel="noopener noreferrer" class="nav-cta" style="margin-top:12px;">
      Check my visibility →
    </a>
  </nav>
</div>

<script>
  const burger  = document.getElementById('nav-hamburger') as HTMLButtonElement;
  const mobile  = document.getElementById('nav-mobile') as HTMLElement;

  burger?.addEventListener('click', () => {
    const open = burger.getAttribute('aria-expanded') === 'true';
    burger.setAttribute('aria-expanded', String(!open));
    burger.setAttribute('aria-label', open ? 'Open navigation' : 'Close navigation');
    mobile.setAttribute('aria-hidden', String(open));
    burger.classList.toggle('is-open', !open);
    mobile.classList.toggle('is-open', !open);
  });

  mobile?.querySelectorAll('a').forEach((a) => {
    a.addEventListener('click', () => {
      burger.setAttribute('aria-expanded', 'false');
      burger.setAttribute('aria-label', 'Open navigation');
      mobile.setAttribute('aria-hidden', 'true');
      burger.classList.remove('is-open');
      mobile.classList.remove('is-open');
    });
  });
</script>

<style>
  .site-nav {
    padding: 14px var(--gutter);
    background: var(--cream);
    border-bottom: 0.5px solid rgba(0, 0, 0, 0.06);
    position: sticky;
    top: 0;
    z-index: 50;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .nav-inner {
    max-width: calc(var(--container) - 2 * var(--gutter));
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .logo { text-decoration: none; display: flex; align-items: center; }

  .nav-links {
    display: flex;
    gap: 22px;
    list-style: none;
    font-size: var(--text-small);
  }

  .nav-link {
    color: var(--navy);
    text-decoration: none;
    opacity: 0.72;
    transition: opacity 0.2s ease;
  }

  .nav-link:hover,
  .nav-link--active { opacity: 1; }

  .nav-link--active { font-weight: 500; }

  .nav-end {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .nav-cta {
    background: var(--amber-deep);
    color: var(--amber-light);
    padding: 12px 20px;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--r-button);
    font-size: var(--text-small);
    font-weight: 500;
    text-decoration: none;
    transition: background 0.2s ease;
    white-space: nowrap;
  }

  .nav-cta:hover { background: #6B3F09; }

  /* Hamburger */
  .hamburger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 4px;
    width: 44px;
    height: 44px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
  }

  .hamburger span {
    display: block;
    width: 100%;
    height: 1.5px;
    background: var(--navy);
    border-radius: 2px;
    transition: transform 0.2s ease, opacity 0.2s ease;
  }

  .hamburger.is-open span:nth-child(1) { transform: translateY(5.5px) rotate(45deg); }
  .hamburger.is-open span:nth-child(2) { opacity: 0; }
  .hamburger.is-open span:nth-child(3) { transform: translateY(-5.5px) rotate(-45deg); }

  /* Mobile drawer */
  .nav-mobile {
    display: none;
    background: var(--cream);
    border-bottom: 0.5px solid rgba(0, 0, 0, 0.06);
    padding: 14px var(--gutter) 18px;
  }

  .nav-mobile nav {
    max-width: calc(var(--container) - 2 * var(--gutter));
    margin: 0 auto;
  }

  .nav-mobile.is-open { display: block; }

  .nav-mobile ul { list-style: none; }

  .nav-mobile-link {
    display: block;
    padding: 14px 0;
    font-size: var(--text-small);
    color: var(--navy);
    text-decoration: none;
    opacity: 0.72;
    border-bottom: 0.5px solid rgba(0, 0, 0, 0.06);
    transition: opacity 0.2s ease;
  }

  .nav-mobile-link:hover,
  .nav-mobile-link.nav-link--active { opacity: 1; }

  @media (max-width: 900px) {
    .nav-links { display: none; }
    .hamburger { display: flex; }
    /* CTA lives in the mobile drawer — hide it from the header bar */
    .nav-end .nav-cta { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav-link, .nav-cta, .hamburger span, .nav-mobile-link {
      transition-duration: 0.01ms;
    }
  }
</style>
