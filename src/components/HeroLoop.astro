---
---

<div class="hl-root">

  <!-- ── AMBER SUB-INDUSTRY BANNER ──────────────── -->
  <div class="subind-header">
    <span class="subind-header-prefix">Top AI searches in:</span>
    <span class="subind-header-name" id="subind-current-name">AVIATION &amp; AEROSPACE</span>
  </div>

  <!-- ── SUB-INDUSTRY TABS ───────────────────────── -->
  <div class="subind-tabs" role="tablist" aria-label="Select sub-industry">
    <button class="subind-tab active" data-sub="commercial" role="tab" aria-selected="true">
      <span class="subind-tab-label">Commercial Aviation</span>
    </button>
    <button class="subind-tab" data-sub="manufacturing" role="tab" aria-selected="false">
      <span class="subind-tab-label">Aerospace Manufacturing</span>
    </button>
    <button class="subind-tab" data-sub="space" role="tab" aria-selected="false">
      <span class="subind-tab-label">Space Exploration</span>
    </button>
    <button class="subind-tab" data-sub="mro" role="tab" aria-selected="false">
      <span class="subind-tab-label">MRO</span>
    </button>
  </div>

  <!-- ── LOOP CONTAINER ─────────────────────────── -->
  <div class="loop-container" id="hl-container">

    <!-- ── STATE 1: Engine search mock ── -->
    <div class="state active" data-state="1">
      <div class="state-header">
        <div>
          <span class="state-tag"><span class="alert-dot"></span>Live AI query · 5 engines</span>
        </div>
        <span class="state-num">01 / 03</span>
      </div>
      <div class="state-body">
        <!-- 5 engine pills -->
        <div class="ec-pills" role="tablist">
          <button class="ec-pill active" data-eidx="0">ChatGPT</button>
          <button class="ec-pill" data-eidx="1">Perplexity</button>
          <button class="ec-pill" data-eidx="2">Gemini</button>
          <button class="ec-pill" data-eidx="3">Copilot</button>
          <button class="ec-pill" data-eidx="4">Claude</button>
        </div>
        <!-- Engine window (JS updates content + CSS class) -->
        <div class="ec-window engine-chatgpt" id="ec-window-main">
          <div class="ec-chrome">
            <div class="ec-chrome-dots"><span></span><span></span><span></span></div>
            <span class="ec-chrome-url" id="ec-chrome-url">chatgpt.com</span>
            <span class="ec-chrome-live"><span class="alert-dot" style="width:5px;height:5px;margin-right:3px;"></span>LIVE</span>
          </div>
          <div class="ec-greeting" id="ec-greeting">What&apos;s on your mind today?</div>
          <div class="ec-input-row">
            <span class="ec-input-icon" id="ec-input-icon">+</span>
            <span class="ec-query-text" id="ec-query-text"></span><span class="typing-cursor" aria-hidden="true"></span>
            <span class="ec-thinking" id="ec-thinking">Thinking <span class="ec-think-anim">›</span></span>
            <span class="ec-input-btn" id="ec-input-btn" aria-hidden="true">●</span>
          </div>
          <div class="ec-answer-area" id="ec-answer-area">
            <span class="ec-answer-label" id="ec-answer-label">ChatGPT</span>
            <p class="ec-answer-text" id="ec-answer-text"></p>
          </div>
        </div>
        <!-- Not cited -->
        <div class="ec-not-cited">
          <span class="ec-nc-label">Your brand</span>
          <span class="ec-nc-status"><span class="x-mark" aria-hidden="true">✗</span> Not cited in any answer</span>
        </div>
        <!-- CTA -->
        <div class="s1-cta">
          <span class="s1-cta-text">Aviation &amp; aerospace buyers screen vendors in AI first.<br/>Find out if yours is being cited.</span>
          <button class="s1-cta-btn" data-goto-state="3">See what AI says →</button>
        </div>
      </div>
    </div>

    <!-- ── STATE 3: Form ── -->
    <div class="state" data-state="3">
      <div class="state-header">
        <div>
          <span class="state-tag"><span class="live-dot"></span>FREE · 5 MIN · NO CALL REQUIRED</span>
          <span class="state-sector">Aviation &amp; Aerospace</span>
        </div>
        <span class="state-num">03 / 03</span>
      </div>
      <div class="state-body">
        <p class="s3-title">Run Your AI Visibility Snapshot</p>
        <p class="s3-sub">Find out where your aviation or aerospace brand stands across all 5 AI engines — results in your inbox in 48 hours.</p>
        <div id="hl-form-wrap-aviation-aerospace">
          <form name="visibility-snapshot" data-netlify="true" data-netlify-honeypot="bot-field" class="snapshot-form" data-sector-form="aviation-aerospace">
            <input type="hidden" name="form-name" value="visibility-snapshot" />
            <input type="hidden" name="sector" value="aviation-aerospace" />
            <input type="hidden" name="source-page" value="homepage-hero" />
            <p class="hl-honeypot" aria-hidden="true"><label>Skip: <input name="bot-field" tabindex="-1" autocomplete="off" /></label></p>
            <div class="s3-field">
              <label class="s3-label" for="hl-av-web">Your website</label>
              <input type="text" class="s3-input" id="hl-av-web" name="website" placeholder="youraviationbrand.com" />
            </div>
            <div class="s3-field">
              <label class="s3-label" for="hl-av-spec">Your aviation or aerospace specialism</label>
              <input type="text" class="s3-input" id="hl-av-spec" name="category" placeholder="e.g. CNS/ATM, MRO, composites, launch" />
            </div>
            <div class="s3-field">
              <label class="s3-label" for="hl-av-q">A question your buyer would ask AI</label>
              <input type="text" class="s3-input" id="hl-av-q" name="buyer-question" placeholder="Best MRO provider for narrowbody jets in Europe?" />
            </div>
            <button type="submit" class="s3-submit">Get my Snapshot →</button>
          </form>
        </div>
        <p class="s3-foot">Tested live across:</p>
        <div class="s3-engines" aria-label="AI engines tested">
          <span class="s3-engine-pill">ChatGPT</span>
          <span class="s3-engine-pill">Claude</span>
          <span class="s3-engine-pill">Copilot</span>
          <span class="s3-engine-pill">Gemini</span>
          <span class="s3-engine-pill">Perplexity</span>
        </div>
      </div>
    </div>

  </div><!-- /loop-container -->

  <!-- ── CONTROLS ────────────────────────────────── -->
  <div class="loop-controls">
    <div class="progress-dots" role="group" aria-label="Slide navigation">
      <div class="dot active" data-target="1" role="button" tabindex="0" aria-label="Go to slide 1"><div class="dot-fill"></div></div>
      <div class="dot" data-target="3" role="button" tabindex="0" aria-label="Go to slide 2"><div class="dot-fill"></div></div>
    </div>
    <div class="loop-meta">
      <span class="hl-play-icon" id="hl-play-icon" aria-hidden="true"></span>
      <span class="hl-pause-icon" id="hl-pause-icon" aria-hidden="true"></span>
      <span id="hl-status">AUTO-ROTATING</span>
    </div>
  </div>

  <p class="loop-narrative" id="hl-narrative" aria-live="polite">
    Aviation &amp; aerospace buyers ask AI to shortlist vendors before any RFP or pitch.
  </p>

</div><!-- /hl-root -->

<script>
(function () {
  const STATE_DURATIONS = { 1: 6500, 3: 14000 };

  const NARRATIVES = {
    1: 'Aviation & aerospace buyers ask AI to shortlist vendors before any RFP or pitch.',
    3: 'Run yours — built specifically for aviation, aerospace, and space brands.',
  };

  const ENGINES = [
    { id: 'chatgpt',    name: 'ChatGPT',    site: 'chatgpt.com',           greeting: "What's on your mind today?",  hasGreeting: true  },
    { id: 'perplexity', name: 'Perplexity', site: 'perplexity.ai',         greeting: '',                            hasGreeting: false },
    { id: 'gemini',     name: 'Gemini',     site: 'gemini.google.com',     greeting: 'Hello. How can I help you?',  hasGreeting: true  },
    { id: 'copilot',    name: 'Copilot',    site: 'copilot.microsoft.com', greeting: '',                            hasGreeting: false },
    { id: 'claude',     name: 'Claude',     site: 'claude.ai',             greeting: "Hi there. I'm Claude.",       hasGreeting: true  },
  ];

  const SUB_INDUSTRIES = {
    commercial:    'Commercial Aviation',
    manufacturing: 'Aerospace Manufacturing',
    space:         'Space Exploration',
    mro:           'MRO',
  };

  const EC_QUERIES = {
    commercial:    'What are the best revenue management platforms for low-cost airlines?',
    manufacturing: 'Leading aerospace composites suppliers for Airbus and Boeing programmes?',
    space:         'Best commercial small satellite launch providers for LEO missions in 2024?',
    mro:           'Top aircraft heavy maintenance MRO facilities in Europe for narrowbody jets?',
  };

  const EC_RESPONSES = {
    commercial: [
      "Amadeus Revenue Management (ARM) and PROS Holdings lead for LCCs. Sabre SynXis and IBS Software are strong alternatives, especially for hybrid carriers like IndiGo and EasyJet.",
      "PROS Revenue Management and Amadeus dominate the LCC space. IBS Software's iFly RMS is gaining at regional carriers. Farelogix is preferred for NDC-native implementations.",
      "For low-cost carriers, PROS Holdings and Amadeus are the top choices. Sabre and Radixx are common at ultra-low-cost carriers like Spirit and Frontier.",
      "Amadeus Revenue Management and PROS are the most recommended for LCCs. IBS Software and Sabre SRM are used by Ryanair, EasyJet and Wizz Air for dynamic pricing.",
      "PROS Holdings and Amadeus ARM are the leading platforms for low-cost carriers. Sabre's Revenue Optimizer and IBS iFly are strong alternatives for mid-size LCCs.",
    ],
    manufacturing: [
      "Hexcel and Toray Advanced Composites dominate aerospace composites for A350 and 787 programmes. Solvay and Cytec supply carbon fibre prepregs for both OEMs.",
      "Toray Industries, Hexcel, and Solvay are the primary composites suppliers for Boeing 787 and Airbus A350. Spirit AeroSystems and Safran are key tier-1 aerostructures integrators.",
      "Hexcel, Toray, and Solvay lead aerospace composites supply for Airbus and Boeing. Teijin and Mitsubishi Chemical are growing their share in carbon fibre supply chains.",
      "Hexcel and Toray are the dominant composites suppliers. Solvay provides advanced materials for A350 fuselage panels. GKN Aerospace and Spirit AeroSystems are major structures integrators.",
      "Hexcel and Toray Advanced Composites supply the majority of carbon fibre for Airbus A350 and Boeing 787. Solvay, Cytec, and Teijin fill secondary supply roles across both programmes.",
    ],
    space: [
      "SpaceX Falcon 9 rideshare dominates LEO small satellite launches. Rocket Lab Electron is the top dedicated small launcher. Exolaunch and D-Orbit provide satellite deployment services.",
      "SpaceX leads with Falcon 9 rideshare at unmatched price points. Rocket Lab is the preferred dedicated small-sat launcher. ISRO's PSLV and Arianespace's Vega-C are competitive for European missions.",
      "SpaceX Transporter missions and Rocket Lab Electron lead small satellite LEO launches. Arianespace's Vega-C and Rocket Lab have the strongest European and Asia-Pacific presence.",
      "SpaceX Falcon 9 rideshare via Transporter missions is the most cost-effective for LEO smallsats. Rocket Lab, Exolaunch, and ISRO are top alternatives for dedicated and rideshare launches.",
      "SpaceX dominates small satellite LEO launches through its Transporter rideshare programme. Rocket Lab Electron is the leading dedicated small launcher. D-Orbit and Exolaunch offer in-orbit deployment services.",
    ],
    mro: [
      "Lufthansa Technik and Air France Industries KLM E&M (AFI KLM) are the largest European MRO providers. ST Engineering and Iberia Maintenance are strong for A320/737 heavy checks.",
      "Lufthansa Technik leads European narrowbody MRO, followed by AFI KLM E&M and SR Technics. TAP Air Portugal MRO is highly competitive on A320 family heavy maintenance costs.",
      "Lufthansa Technik and AFI KLM E&M dominate European heavy maintenance. Iberia Maintenance, SR Technics, and HAECO are major competitors for A320 and 737 C and D checks.",
      "Lufthansa Technik is Europe's largest MRO. AFI KLM E&M, SR Technics, and Iberia Maintenance are the top competitors for narrowbody heavy checks. Turkish Technic is growing rapidly.",
      "Lufthansa Technik and AFI KLM Engineering & Maintenance lead European heavy MRO. SR Technics and Iberia Maintenance are top choices for A320 family C-checks at competitive price points.",
    ],
  };

  // ── State ──────────────────────────────────────
  let currentSub = 'commercial';
  let currentState = 1;
  let currentEngine = 0;
  let engineTimer = null;
  let engineTypingTimer = null;
  let autoplayActive = true;
  let progressInterval = null;
  let progress = 0;
  let formEngaged = false;
  let resumeTimer = null;

  const container  = document.getElementById('hl-container');
  const statusEl   = document.getElementById('hl-status');
  const playIcon   = document.getElementById('hl-play-icon');
  const pauseIcon  = document.getElementById('hl-pause-icon');
  const narrative  = document.getElementById('hl-narrative');
  const subindName = document.getElementById('subind-current-name');

  // ── Engine functions ────────────────────────────

  function setEngine(eidx, skipAnimation) {
    currentEngine = eidx;
    const eng = ENGINES[eidx];

    // Pills
    document.querySelectorAll('.ec-pill').forEach(function(p, i) {
      p.classList.toggle('active', i === eidx);
    });

    // Window class
    const win = document.getElementById('ec-window-main');
    if (win) {
      win.className = 'ec-window engine-' + eng.id;
    }

    // Chrome URL
    const urlEl = document.getElementById('ec-chrome-url');
    if (urlEl) urlEl.textContent = eng.site;

    // Answer label
    const labelEl = document.getElementById('ec-answer-label');
    if (labelEl) labelEl.textContent = eng.name;

    // Greeting visibility
    const greetingEl = document.getElementById('ec-greeting');
    if (greetingEl) {
      greetingEl.textContent = eng.greeting;
      greetingEl.style.display = eng.hasGreeting ? 'block' : 'none';
    }

    // Reset animation state
    const queryEl    = document.getElementById('ec-query-text');
    const thinkingEl = document.getElementById('ec-thinking');
    const answerArea = document.getElementById('ec-answer-area');
    const answerText = document.getElementById('ec-answer-text');

    if (!queryEl || !thinkingEl || !answerArea || !answerText) return;

    if (engineTypingTimer) { clearInterval(engineTypingTimer); engineTypingTimer = null; }

    queryEl.textContent = '';
    thinkingEl.classList.remove('is-thinking');
    answerArea.classList.remove('is-visible');
    answerText.textContent = '';

    if (skipAnimation) {
      queryEl.textContent = EC_QUERIES[currentSub];
      thinkingEl.classList.add('is-thinking');
      answerText.textContent = EC_RESPONSES[currentSub][eidx];
      answerArea.classList.add('is-visible');
      return;
    }

    // Type query char by char
    const query = EC_QUERIES[currentSub];
    let charIdx = 0;
    engineTypingTimer = setInterval(function() {
      if (charIdx < query.length) {
        queryEl.textContent = query.slice(0, ++charIdx);
      } else {
        clearInterval(engineTypingTimer);
        engineTypingTimer = null;
        setTimeout(function() {
          thinkingEl.classList.add('is-thinking');
          setTimeout(function() {
            answerText.textContent = EC_RESPONSES[currentSub][eidx];
            answerArea.classList.add('is-visible');
          }, 900);
        }, 200);
      }
    }, 45);
  }

  function startEngineLoop() {
    if (engineTimer) clearInterval(engineTimer);
    engineTimer = setInterval(function() {
      if (currentState !== 1) return;
      currentEngine = (currentEngine + 1) % ENGINES.length;
      setEngine(currentEngine, false);
    }, 5500);
  }

  function stopEngineLoop() {
    if (engineTimer) { clearInterval(engineTimer); engineTimer = null; }
    if (engineTypingTimer) { clearInterval(engineTypingTimer); engineTypingTimer = null; }
  }

  // ── Main render ────────────────────────────────

  function render() {
    // Show/hide states
    document.querySelectorAll('.state').forEach(function(el) {
      const sn = parseInt(el.dataset.state || '0');
      el.classList.toggle('active', sn === currentState);
    });

    // Dots
    document.querySelectorAll('.dot').forEach(function(el) {
      const t = parseInt(el.dataset.target || '0');
      el.classList.toggle('active', t === currentState);
      const fill = el.querySelector('.dot-fill');
      if (fill) fill.style.width = t < currentState ? '100%' : '0%';
    });

    // Narrative
    if (narrative) narrative.textContent = NARRATIVES[currentState] || '';
    progress = 0;

    // Engine carousel
    if (currentState === 1) {
      currentEngine = 0;
      setEngine(0, false);
      startEngineLoop();
    } else {
      stopEngineLoop();
    }
  }

  function advance() {
    currentState = currentState === 1 ? 3 : 1;
    render();
  }

  function startAutoplay() {
    if (formEngaged) return;
    autoplayActive = true;
    if (statusEl) statusEl.textContent = 'AUTO-ROTATING';
    if (playIcon)  playIcon.style.display  = 'inline-block';
    if (pauseIcon) pauseIcon.style.display = 'none';
    if (progressInterval) clearInterval(progressInterval);

    progressInterval = setInterval(function() {
      if (!autoplayActive || formEngaged) return;
      progress += 100;
      const total = STATE_DURATIONS[currentState] || 6500;
      const pct = Math.min(100, (progress / total) * 100);
      const activeFill = document.querySelector('.dot.active .dot-fill');
      if (activeFill) activeFill.style.width = pct + '%';
      if (progress >= total) {
        progress = 0;
        advance();
      }
    }, 100);
  }

  function pauseAutoplay(reason) {
    autoplayActive = false;
    if (progressInterval) { clearInterval(progressInterval); progressInterval = null; }
    if (statusEl) statusEl.textContent = reason;
    if (playIcon)  playIcon.style.display  = 'none';
    if (pauseIcon) pauseIcon.style.display = 'inline-block';
  }

  function scheduleResume() {
    if (resumeTimer) clearTimeout(resumeTimer);
    resumeTimer = setTimeout(function() { if (!formEngaged) startAutoplay(); }, 8000);
  }

  // ── Sub-industry tab clicks ────────────────────
  document.querySelectorAll('.subind-tab').forEach(function(tab) {
    tab.addEventListener('click', function() {
      const sub = tab.dataset.sub || 'commercial';
      currentSub = sub;

      // Update active tab
      document.querySelectorAll('.subind-tab').forEach(function(t) {
        const isActive = t.dataset.sub === sub;
        t.classList.toggle('active', isActive);
        t.setAttribute('aria-selected', String(isActive));
      });


      // Reset to state 1 with new sub-industry query
      currentState = 1;
      currentEngine = 0;
      render();
      pauseAutoplay('SUB-INDUSTRY SELECTED');
      scheduleResume();
    });
  });

  // ── Dot clicks ────────────────────────────────
  document.querySelectorAll('.dot').forEach(function(dot) {
    const activate = function() {
      currentState = parseInt(dot.dataset.target || '1');
      render();
      pauseAutoplay('YOU NAVIGATED');
      if (currentState !== 3) scheduleResume();
    };
    dot.addEventListener('click', activate);
    dot.addEventListener('keydown', function(e) {
      if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); activate(); }
    });
  });

  // ── CTA buttons (states 1 & 2 → state 3) ──────
  document.querySelectorAll('[data-goto-state]').forEach(function(btn) {
    btn.addEventListener('click', function() {
      const target = parseInt(btn.dataset.gotoState || '3');
      currentState = target;
      render();
      pauseAutoplay('YOU NAVIGATED');
    });
  });

  // ── Engine pill clicks ────────────────────────
  document.querySelectorAll('.ec-pill').forEach(function(pill) {
    pill.addEventListener('click', function() {
      stopEngineLoop();
      setEngine(parseInt(pill.dataset.eidx || '0'), false);
      startEngineLoop();
      pauseAutoplay('ENGINE SELECTED');
      scheduleResume();
    });
  });

  // ── Hover pause ───────────────────────────────
  if (container) {
    container.addEventListener('mouseenter', function() {
      if (!formEngaged && autoplayActive) pauseAutoplay('HOVER PAUSE');
    });
    container.addEventListener('mouseleave', function() {
      if (!formEngaged) startAutoplay();
    });
  }

  // ── Form engagement → permanent pause ─────────
  document.querySelectorAll('.snapshot-form').forEach(function(form) {
    form.addEventListener('focusin', function() {
      formEngaged = true;
      pauseAutoplay('FORM ACTIVE');
    });
    form.addEventListener('input', function() {
      formEngaged = true;
      pauseAutoplay('FORM ACTIVE');
    });

    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      formEngaged = true;
      pauseAutoplay('FORM ACTIVE');
      const wrapId = 'hl-form-wrap-aviation-aerospace';
      const wrap = document.getElementById(wrapId);
      const body = new URLSearchParams(
        Array.from(new FormData(form)).map(function([k, v]) { return [k, v.toString()]; }),
      ).toString();
      try {
        await fetch('/', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body,
        });
        if (wrap) wrap.innerHTML = '<p class="hl-thanks">Thanks — we\'ll analyse your AI visibility and send results within one business day.</p>';
      } catch {
        form.submit();
      }
    });
  });

  // ── Boot ──────────────────────────────────────
  render();
  startAutoplay();
})();
</script>

<style>
  .hl-honeypot { position: absolute; left: -9999px; }

  /* ── Sub-industry amber banner ──────────────── */
  .subind-header {
    background: var(--amber);
    color: var(--amber-light);
    padding: 10px 16px;
    border-radius: 8px 8px 0 0;
    font-size: 13px;
    font-weight: 500;
    font-family: var(--font-sans);
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .subind-header-prefix { opacity: 0.8; }
  .subind-header-name { font-weight: 600; }

  /* ── Sub-industry tabs ──────────────────────── */
  .subind-tabs {
    display: flex;
    background: var(--cream);
    border: 0.5px solid var(--line);
    border-top: none;
    padding: 4px;
    gap: 2px;
    margin-bottom: 14px;
  }

  .subind-tab {
    flex: 1;
    background: transparent;
    border: none;
    padding: 9px 8px;
    color: var(--muted);
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    cursor: pointer;
    border-radius: 4px;
    transition: all 0.2s ease;
    font-family: var(--font-sans);
    text-align: center;
  }

  .subind-tab:hover { color: var(--navy); background: rgba(4,44,83,0.04); }

  .subind-tab.active {
    background: var(--navy);
    color: var(--amber-light);
  }

  .subind-tab-label { font-size: 10px; }

  /* ── Loop container ─────────────────────────── */
  .loop-container {
    position: relative;
    min-height: 490px;
  }

  /* ── State card ─────────────────────────────── */
  .state {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 30px 60px -20px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(239, 159, 39, 0.18);
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px) scale(0.97);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .state.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0) scale(1);
  }

  /* State 1: navy engine-test card */
  .state[data-state="1"] { background: var(--navy-deep); }
  .state[data-state="1"] .state-header {
    background: var(--navy-deep);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }
  .state[data-state="1"] .state-num    { color: var(--blue-mid); }
  .state[data-state="1"] .s1-cta { border-top-color: rgba(255, 255, 255, 0.08); }
  .state[data-state="1"] .s1-cta-text { color: var(--blue-mid); }
  .state[data-state="1"] .s1-cta-btn { background: var(--amber); color: var(--amber-light); }
  .state[data-state="1"] .s1-cta-btn:hover { background: #854F0B; }

  /* ── State header ───────────────────────────── */
  .state-header {
    padding: 13px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid rgba(4, 44, 83, 0.08);
    background: linear-gradient(180deg, #FCFAF6 0%, #F8F4ED 100%);
  }

  .state-tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 10px;
    color: var(--amber);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-weight: 500;
  }

  .live-dot,
  .alert-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    display: inline-block;
    flex-shrink: 0;
    animation: hl-pulse 1.8s infinite;
  }

  .live-dot  { background: #1D9E75; }
  .alert-dot { background: #DC2626; }

  @keyframes hl-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%       { opacity: 0.6; transform: scale(1.2); }
  }

  .state-num {
    font-size: 10px;
    color: var(--muted);
    letter-spacing: 0.08em;
    font-weight: 500;
    font-family: var(--font-mono);
  }

  .state-sector {
    font-size: 9px;
    color: var(--amber);
    background: var(--amber-light);
    padding: 3px 7px;
    border-radius: 3px;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-left: 8px;
  }

  /* ── State body shared ──────────────────────── */
  .state-body { padding: 22px 24px; }

  /* ── Engine pills ───────────────────────────── */
  .ec-pills {
    display: flex;
    gap: 4px;
    margin-bottom: 10px;
    flex-wrap: wrap;
  }

  .ec-pill {
    padding: 4px 9px;
    font-size: 10px;
    font-weight: 500;
    font-family: var(--font-sans);
    border-radius: 3px;
    border: 0.5px solid rgba(255,255,255,0.15);
    background: transparent;
    color: var(--blue-mid);
    cursor: pointer;
    transition: all 0.15s ease;
    letter-spacing: 0.03em;
  }

  .ec-pill:hover { border-color: rgba(255,255,255,0.3); color: #fff; }
  .ec-pill.active { background: rgba(255,255,255,0.12); color: #fff; border-color: rgba(255,255,255,0.25); }

  /* ── Engine window base ─────────────────────── */
  .ec-window {
    border-radius: 8px;
    overflow: hidden;
    margin-bottom: 10px;
    border: 0.5px solid rgba(255,255,255,0.08);
    transition: background 0.3s ease;
  }

  .ec-chrome {
    padding: 8px 12px;
    display: flex;
    align-items: center;
    gap: 8px;
    border-bottom: 0.5px solid rgba(255,255,255,0.08);
  }

  .ec-chrome-dots { display: flex; gap: 4px; }

  .ec-chrome-dots span {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    display: block;
  }

  .ec-chrome-dots span:nth-child(1) { background: #FF5F57; }
  .ec-chrome-dots span:nth-child(2) { background: #FFBD2E; }
  .ec-chrome-dots span:nth-child(3) { background: #28C840; }

  .ec-chrome-url {
    flex: 1;
    text-align: center;
    font-size: 10px;
    color: rgba(255,255,255,0.35);
    font-family: var(--font-mono);
  }

  .ec-chrome-live {
    font-size: 9px;
    color: rgba(255,255,255,0.4);
    letter-spacing: 0.08em;
    display: flex;
    align-items: center;
  }

  .ec-greeting {
    padding: 14px 14px 6px;
    font-size: 15px;
    font-weight: 400;
    text-align: center;
    color: rgba(255,255,255,0.75);
    font-family: var(--font-sans);
    line-height: 1.3;
  }

  .ec-input-row {
    padding: 8px 12px 10px;
    display: flex;
    align-items: center;
    gap: 8px;
    border-top: 0.5px solid rgba(255,255,255,0.06);
  }

  .ec-input-icon {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,0.2);
    color: rgba(255,255,255,0.5);
    font-size: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    line-height: 1;
  }

  .ec-query-text {
    flex: 1;
    font-size: 12px;
    color: rgba(255,255,255,0.85);
    font-family: var(--font-sans);
    min-height: 18px;
  }

  .typing-cursor {
    display: inline-block;
    width: 2px;
    height: 14px;
    background: rgba(255,255,255,0.7);
    animation: hl-blink 0.7s infinite;
    flex-shrink: 0;
    vertical-align: middle;
  }

  @keyframes hl-blink {
    0%, 49% { opacity: 1; }
    50%, 100% { opacity: 0; }
  }

  .ec-thinking {
    font-size: 10px;
    color: rgba(255,255,255,0.35);
    white-space: nowrap;
    opacity: 0;
    transition: opacity 0.3s;
    font-family: var(--font-sans);
  }

  .ec-thinking.is-thinking { opacity: 1; }
  .ec-think-anim { animation: hl-blink 0.9s infinite; }

  .ec-input-btn {
    width: 26px;
    height: 26px;
    border-radius: 50%;
    background: rgba(255,255,255,0.12);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 10px;
    color: rgba(255,255,255,0.4);
    flex-shrink: 0;
  }

  .ec-answer-area {
    padding: 10px 14px 12px;
    border-top: 0.5px solid rgba(255,255,255,0.06);
    opacity: 0;
    transition: opacity 0.5s ease;
  }

  .ec-answer-area.is-visible { opacity: 1; }

  .ec-answer-label {
    font-size: 9px;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    display: block;
    margin-bottom: 5px;
    font-family: var(--font-mono);
  }

  .ec-answer-text {
    font-size: 11px;
    color: rgba(255,255,255,0.72);
    line-height: 1.55;
    font-family: var(--font-sans);
    margin: 0;
  }

  /* ── Per-engine overrides ───────────────────── */

  /* ChatGPT */
  .engine-chatgpt { background: #212121; }
  .engine-chatgpt .ec-chrome { background: #1A1A1A; }
  .engine-chatgpt .ec-greeting { color: #ECECEC; }
  .engine-chatgpt .ec-input-row {
    background: #2F2F2F;
    border-radius: 20px;
    margin: 0 12px 10px;
    border: 0.5px solid rgba(255,255,255,0.12);
    border-top: none;
  }
  .engine-chatgpt .ec-input-icon { color: #fff; border-color: rgba(255,255,255,0.3); }
  .engine-chatgpt .ec-input-btn { background: #000; color: #fff; font-size: 12px; }
  .engine-chatgpt .ec-answer-label { color: #10A37F; }

  /* Perplexity */
  .engine-perplexity { background: #1C1C1C; }
  .engine-perplexity .ec-chrome { background: #141414; }
  .engine-perplexity .ec-greeting { display: none; }
  .engine-perplexity .ec-input-row {
    padding: 14px;
    border: 0.5px solid rgba(255,255,255,0.1);
    margin: 10px 12px;
    border-radius: 8px;
    border-top: 0.5px solid rgba(255,255,255,0.1);
  }
  .engine-perplexity .ec-input-icon { border-color: #20B2AA; color: #20B2AA; font-size: 12px; }
  .engine-perplexity .ec-input-btn { background: #20B2AA; color: #fff; }
  .engine-perplexity .ec-answer-label { color: #20B2AA; }
  .engine-perplexity .ec-thinking { color: rgba(32,178,170,0.6); }

  /* Gemini */
  .engine-gemini { background: #1E1F23; }
  .engine-gemini .ec-chrome { background: #17181C; }
  .engine-gemini .ec-greeting { color: #E3E3E3; }
  .engine-gemini .ec-input-row {
    background: #2D2E33;
    border-radius: 24px;
    margin: 0 12px 10px;
    border: 0.5px solid rgba(255,255,255,0.08);
    border-top: none;
  }
  .engine-gemini .ec-input-btn { background: linear-gradient(135deg, #4285F4 0%, #A142F4 100%); color: #fff; font-size: 11px; }
  .engine-gemini .ec-answer-label { color: #4285F4; }
  .engine-gemini .ec-thinking { color: rgba(66,133,244,0.6); }

  /* Copilot */
  .engine-copilot { background: #1A1A2E; }
  .engine-copilot .ec-chrome { background: #14142A; }
  .engine-copilot .ec-greeting { display: none; }
  .engine-copilot .ec-input-row {
    background: #252545;
    border-radius: 6px;
    margin: 10px 12px;
    border: 1px solid rgba(14,165,233,0.2);
    border-top: 1px solid rgba(14,165,233,0.2);
  }
  .engine-copilot .ec-input-icon { border-color: #0EA5E9; color: #0EA5E9; }
  .engine-copilot .ec-input-btn { background: #0EA5E9; color: #fff; }
  .engine-copilot .ec-answer-label { color: #0EA5E9; }
  .engine-copilot .ec-thinking { color: rgba(14,165,233,0.6); }

  /* Claude */
  .engine-claude { background: #1F1B18; }
  .engine-claude .ec-chrome { background: #181410; }
  .engine-claude .ec-greeting { color: #EDE9E4; }
  .engine-claude .ec-input-row {
    background: #2D2820;
    border-radius: 8px;
    margin: 0 12px 10px;
    border: 0.5px solid rgba(218,119,86,0.2);
    border-top: none;
  }
  .engine-claude .ec-input-icon { border-color: #DA7756; color: #DA7756; }
  .engine-claude .ec-input-btn { background: #DA7756; color: #fff; }
  .engine-claude .ec-answer-label { color: #DA7756; }
  .engine-claude .ec-thinking { color: rgba(218,119,86,0.6); }

  /* ── Not cited row ──────────────────────────── */
  .ec-not-cited {
    background: rgba(220,38,38,0.1);
    border: 0.5px solid rgba(220,38,38,0.3);
    border-radius: 6px;
    padding: 9px 12px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }

  .ec-nc-label { font-weight: 500; color: #FCA5A5; font-size: 11px; }

  .ec-nc-status {
    color: #FCA5A5;
    font-size: 11px;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 5px;
  }

  .x-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 15px;
    height: 15px;
    border-radius: 50%;
    background: #DC2626;
    color: #fff;
    font-size: 9px;
    font-weight: 500;
  }

  /* ── S1 CTA row ─────────────────────────────── */
  .s1-cta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    padding-top: 12px;
    border-top: 1px solid rgba(4,44,83,0.08);
  }

  .s1-cta-text { font-size: 11px; color: var(--muted); line-height: 1.4; }

  .s1-cta-btn {
    background: var(--navy);
    color: #fff;
    padding: 9px 14px;
    border-radius: 5px;
    font-size: 11px;
    font-weight: 500;
    border: none;
    cursor: pointer;
    white-space: nowrap;
    font-family: var(--font-sans);
    transition: background 0.2s ease;
    flex-shrink: 0;
  }

  .s1-cta-btn:hover { background: var(--navy-deep); }

  /* ── State 2: snapshot sample ───────────────── */
  .s2-client {
    font-size: 10px;
    color: var(--amber);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-weight: 500;
    margin-bottom: 5px;
  }

  .s2-title {
    font-size: 18px;
    font-weight: 500;
    color: var(--navy);
    margin-bottom: 14px;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .s2-title .num { color: var(--amber); }

  .s2-stats {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 8px;
    margin-bottom: 14px;
  }

  .s2-stat {
    background: var(--cream);
    border-radius: 7px;
    padding: 10px;
    text-align: center;
  }

  .s2-stat-num {
    font-size: 20px;
    font-weight: 500;
    color: var(--navy);
    line-height: 1;
    margin-bottom: 4px;
    letter-spacing: -0.02em;
    font-family: var(--font-mono);
  }

  .up { color: var(--signal); font-size: 10px; font-weight: 500; margin-left: 2px; }

  .s2-stat-label {
    font-size: 9px;
    color: var(--muted);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-weight: 500;
  }

  .s2-fixes-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 7px;
  }

  .s2-fixes-label {
    font-size: 10px;
    color: var(--amber);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-weight: 500;
  }

  .s2-priority {
    font-size: 9px;
    background: var(--amber-light);
    color: var(--amber);
    padding: 3px 7px;
    border-radius: 8px;
    font-weight: 500;
    letter-spacing: 0.05em;
  }

  .s2-fix {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    padding: 7px 0;
    border-bottom: 1px solid rgba(4,44,83,0.07);
    font-size: 12px;
    animation: hl-fade-row 0.5s ease backwards;
  }

  .s2-fix:last-of-type { border: none; }

  .state[data-state="2"].active .s2-fix:nth-of-type(1) { animation-delay: 0.4s; }
  .state[data-state="2"].active .s2-fix:nth-of-type(2) { animation-delay: 0.7s; }
  .state[data-state="2"].active .s2-fix:nth-of-type(3) { animation-delay: 1s; }

  @keyframes hl-fade-row {
    from { opacity: 0; transform: translateX(-8px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  .s2-fix-num {
    background: var(--navy);
    color: #fff;
    width: 19px;
    height: 19px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 9px;
    font-weight: 500;
    flex-shrink: 0;
    margin-top: 1px;
  }

  .s2-fix-title { color: var(--navy); font-weight: 500; font-size: 11px; margin-bottom: 1px; }
  .s2-fix-impact { color: var(--muted); font-size: 10px; }
  .lift { color: var(--signal); font-weight: 500; }

  .s2-cta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 10px;
    margin-top: 4px;
    border-top: 1px solid rgba(4,44,83,0.08);
  }

  .s2-cta-text { font-size: 11px; color: var(--muted); }

  .s2-cta-btn {
    background: var(--amber);
    color: #fff;
    padding: 9px 14px;
    border-radius: 5px;
    font-size: 11px;
    font-weight: 500;
    border: none;
    cursor: pointer;
    white-space: nowrap;
    font-family: var(--font-sans);
    transition: background 0.2s ease;
    flex-shrink: 0;
  }

  .s2-cta-btn:hover { background: #854F0B; }

  /* ── State 3: form ──────────────────────────── */
  .s3-title {
    font-size: 18px;
    font-weight: 500;
    color: var(--navy);
    margin-bottom: 5px;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .s3-sub {
    font-size: 11px;
    color: var(--muted);
    margin-bottom: 14px;
    line-height: 1.5;
  }

  .s3-field { margin-bottom: 10px; }

  .s3-label {
    font-size: 10px;
    color: var(--muted);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 500;
    display: block;
    margin-bottom: 4px;
  }

  .s3-input {
    width: 100%;
    border: 1px solid rgba(4,44,83,0.15);
    border-radius: 5px;
    padding: 8px 11px;
    font-size: 12px;
    background: var(--cream);
    color: var(--navy);
    font-family: var(--font-sans);
    outline: none;
    box-sizing: border-box;
    transition: border-color 0.2s ease, background 0.2s ease;
  }

  .s3-input:focus { border-color: var(--amber); background: #fff; }
  .s3-input::placeholder { color: rgba(4,44,83,0.4); }

  .s3-submit {
    width: 100%;
    background: var(--amber);
    color: var(--amber-light);
    padding: 11px;
    border: none;
    border-radius: 5px;
    font-size: 12px;
    font-weight: 500;
    cursor: pointer;
    margin-top: 4px;
    font-family: var(--font-sans);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 5px;
    letter-spacing: 0.02em;
    transition: background 0.2s ease;
  }

  .s3-submit:hover { background: #854F0B; }

  .s3-foot {
    font-size: 10px;
    color: var(--muted);
    margin-top: 10px;
    text-align: center;
    line-height: 1.4;
  }

  .s3-engines {
    display: flex;
    justify-content: center;
    gap: 4px;
    margin-top: 7px;
    flex-wrap: wrap;
  }

  .s3-engine-pill {
    font-size: 9px;
    font-weight: 500;
    color: var(--navy);
    background: var(--cream);
    padding: 3px 7px;
    border-radius: 3px;
    letter-spacing: 0.03em;
  }

  /* ── Form thank-you ─────────────────────────── */
  .hl-thanks {
    font-size: 13px;
    color: var(--navy);
    line-height: 1.55;
    padding: 12px 0 4px;
    font-weight: 500;
  }

  /* ── Controls ───────────────────────────────── */
  .loop-controls {
    margin-top: 14px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
  }

  .progress-dots { display: flex; gap: 6px; }

  .dot {
    width: 28px;
    height: 3px;
    background: rgba(255,255,255,0.15);
    border-radius: 2px;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: background 0.3s ease;
  }

  .dot.active { background: rgba(255,255,255,0.22); }
  .dot:hover  { background: rgba(255,255,255,0.3); }

  .dot-fill {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 0;
    background: var(--amber-mid);
    transition: width 0.1s linear;
  }

  .loop-meta {
    font-size: 10px;
    color: var(--blue-mid);
    letter-spacing: 0.08em;
    display: flex;
    align-items: center;
    gap: 6px;
    font-weight: 500;
  }

  /* play ▶ triangle */
  .hl-play-icon {
    display: inline-block;
    width: 0;
    height: 0;
    border-left: 6px solid currentColor;
    border-top: 4px solid transparent;
    border-bottom: 4px solid transparent;
  }

  /* pause ‖ two bars */
  .hl-pause-icon {
    display: none;
    position: relative;
    width: 7px;
    height: 9px;
  }

  .hl-pause-icon::before,
  .hl-pause-icon::after {
    content: '';
    position: absolute;
    width: 2px;
    height: 9px;
    background: currentColor;
    top: 0;
  }

  .hl-pause-icon::before { left: 0; }
  .hl-pause-icon::after  { right: 0; }

  /* ── Narrative ──────────────────────────────── */
  .loop-narrative {
    margin-top: 12px;
    text-align: center;
    font-size: 11px;
    color: var(--blue-mid);
    font-style: italic;
    letter-spacing: 0.02em;
    min-height: 18px;
    transition: opacity 0.3s ease;
  }

  /* ── Responsive ─────────────────────────────── */
  @media (max-width: 640px) {
    .subind-tab-label { display: none; }
    .loop-container { min-height: 520px; }
    .ec-pill { font-size: 8px; padding: 2px 6px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .state            { transition-duration: 0.01ms; }
    .loop-narrative   { transition-duration: 0.01ms; }
    .live-dot, .alert-dot, .typing-cursor, .ec-think-anim { animation-duration: 0.01ms; }
    .s2-fix           { animation-duration: 0.01ms; }
    .s1-cta-btn, .s2-cta-btn, .s3-submit, .s3-input { transition-duration: 0.01ms; }
    .ec-window, .ec-answer-area, .ec-thinking, .ec-pill { transition-duration: 0.01ms; }
    .dot, .dot-fill   { transition-duration: 0.01ms; }
    .subind-tab       { transition-duration: 0.01ms; }
  }
</style>
