---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ValueCalculator from '../../components/sections/ValueCalculator.astro';
---

<BaseLayout
  title="Build AI Authority | 3-stage Visibility Engine — Maxifi Digital"
  description="Get cited by ChatGPT, Claude, Perplexity, Gemini and Copilot. Three-stage AEO programme: Snapshot baseline, Sprint implementation, Monitor & repeat."
  currentPage="/work"
  ogImageAlt="Build AI Authority — 3-stage Visibility Engine for AI citation"
>
  <Fragment slot="head">
    <script type="application/ld+json" is:inline>{
  "@context": "https://schema.org",
  "@type": "Service",
  "name": "Visibility Engine",
  "provider": {
    "@type": "Organization",
    "name": "Maxifi Digital",
    "url": "https://maxifidigital.com"
  },
  "description": "The Visibility Engine is Maxifi Digital's core AEO programme, a three-stage system — Snapshot, Sprint, Monitor — that builds and sustains AI citation authority for expert-led brands. It structures existing expertise into machine-readable, citation-optimised content assets that AI engines can retrieve, attribute, and surface in buyer queries.",
  "url": "https://maxifidigital.com/work",
  "areaServed": ["Singapore", "Global"]
}</script>
  </Fragment>

  <!-- ══════════════════════════════════════════
       SYSTEM — THE 3-STAGE VISIBILITY ENGINE
  ══════════════════════════════════════════ -->
  <section class="system-section section--navy" aria-labelledby="system-heading">
    <div class="wrap-inner">
      <p class="system-eyebrow">BUILD AI AUTHORITY</p>
      <div class="system-head">
        <h2 id="system-heading" class="system-title">The Visibility Engine — a 3-stage system that <em>compounds</em></h2>
        <p class="system-description">Not a content sprint. Not a one-off audit. A managed system that takes your expertise from invisible to indexed, then keeps citation share growing — quarter on quarter.</p>
      </div>

      <div class="system-grid">
        <div class="stages">
          <div class="stage">
            <div class="stage-label">Stage 01<br>Visibility Snapshot</div>
            <div class="stage-content">
              <h3 class="stage-title">See where AI cites you today</h3>
              <p class="stage-body">We map your citation share across all five engines and 25–50 buyer questions in your category. You get the three highest-leverage fixes within fourteen days.</p>
            </div>
          </div>
          <div class="stage">
            <div class="stage-label">Stage 02<br>Visibility Report</div>
            <div class="stage-content">
              <h3 class="stage-title">Ship answer-ready assets</h3>
              <p class="stage-body">We build the Q&amp;A layer, schema scaffolding and entity files AI engines need to cite you cleanly. Senior strategist embedded throughout.</p>
            </div>
          </div>
          <div class="stage">
            <div class="stage-label">Stage 03<br>Visibility Engine</div>
            <div class="stage-content">
              <h3 class="stage-title">Monitor, lift, defend</h3>
              <p class="stage-body">Weekly citation monitoring, quarterly Snapshot, monthly strategy review. Each cited answer trains the next — the curve accelerates.</p>
            </div>
          </div>
        </div>

        <div class="snap-card" aria-label="Sample Snapshot preview">
          <div class="snap-hdr">
            <h4 class="snap-title">Snapshot · Acme Consulting</h4>
            <span class="snap-score">78<em>/100</em></span>
          </div>
          <div class="snap-row"><span>Best firm for healthcare M&amp;A · ChatGPT</span><span class="snap-value snap-value--cited">cited · #2</span></div>
          <div class="snap-row"><span>Best firm for healthcare M&amp;A · Claude</span><span class="snap-value snap-value--cited">cited · #1</span></div>
          <div class="snap-row"><span>Best firm for healthcare M&amp;A · Perplexity</span><span class="snap-value snap-value--not">not cited</span></div>
          <div class="snap-row"><span>Healthcare regulatory advisors · Gemini</span><span class="snap-value snap-value--cited">cited · #4</span></div>
          <div class="snap-row"><span>Top M&amp;A litigation specialists · Copilot</span><span class="snap-value snap-value--not">not cited</span></div>
          <div class="snap-row"><span>Cross-border health regulation · ChatGPT</span><span class="snap-value snap-value--cited">cited · #3</span></div>
          <div class="snap-legend">
            <span><i class="legend-dot legend-dot--cited"></i>Cited</span>
            <span><i class="legend-dot legend-dot--not"></i>Not cited</span>
            <span>· 6 of 25 queries shown</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       VALUE MODEL — CALCULATOR
  ══════════════════════════════════════════ -->
  <section class="section" id="value-model" aria-labelledby="work-value-heading">
    <div class="wrap-inner">
      <p class="s-eyebrow vv-eyebrow">THE VALUE MODEL</p>
      <h2 id="work-value-heading" class="section-h2">Put a number on citation share</h2>
      <p class="vv-intro">
        The MAXIFI Visibility Value Model prices AI citations from six numbers your pipeline
        already has. Replace the placeholder defaults with your records and read the result
        live — <a href="/visibility-value" class="vv-link">see how the model works →</a>
      </p>
      <ValueCalculator preset="enterprise" />
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       CTA
  ══════════════════════════════════════════ -->
  <section class="section section--pt0" aria-labelledby="work-cta-heading">
    <div class="wrap-inner">
      <div class="cta-box">
        <p class="s-eyebrow cta-eyebrow">START THE ENGINE</p>
        <h2 id="work-cta-heading" class="cta-title">
          Not sure where to start? Begin with a Visibility Snapshot.
        </h2>
        <p class="cta-body">
          The snapshot is the entry point — it tells you exactly where you stand and what a Visibility Sprint or Engine would address. No obligation to continue.
        </p>
        <div class="cta-buttons">
          <a href="https://checkyourvisibility.maxifidigital.com/" target="_blank" rel="noopener noreferrer" class="btn-primary">Get my free Visibility Snapshot</a>
          <a href="https://aswhub.maxifidigital.com/" target="_blank" rel="noopener noreferrer" class="btn-outline">See how Conference AEO operates</a>
        </div>
      </div>
    </div>
  </section>

  <script type="application/ld+json" is:inline>
  {
    "@context": "https://schema.org",
    "@type": "Service",
    "name": "Visibility Engine — AEO Retainer",
    "provider": {
      "@type": "Organization",
      "name": "Maxifi Digital",
      "url": "https://maxifidigital.com"
    },
    "description": "A quarterly AEO retainer covering Snapshot audits, content Sprints, citation monitoring and strategy calls across five AI engines.",
    "offers": [
      {
        "@type": "Offer",
        "name": "Engine Core",
        "price": "7500",
        "priceCurrency": "SGD",
        "priceSpecification": {
          "@type": "UnitPriceSpecification",
          "price": "7500",
          "priceCurrency": "SGD",
          "unitText": "month"
        }
      },
      {
        "@type": "Offer",
        "name": "Engine Plus",
        "price": "12750",
        "priceCurrency": "SGD",
        "priceSpecification": {
          "@type": "UnitPriceSpecification",
          "price": "12750",
          "priceCurrency": "SGD",
          "unitText": "month"
        }
      },
      {
        "@type": "Offer",
        "name": "Engine Enterprise",
        "price": "18000",
        "priceCurrency": "SGD",
        "priceSpecification": {
          "@type": "UnitPriceSpecification",
          "price": "18000",
          "priceCurrency": "SGD",
          "unitText": "month"
        }
      }
    ]
  }
  </script>

</BaseLayout>

<style>
  .wrap-inner {
    max-width: var(--container);
    margin: 0 auto;
    padding: 0 var(--gutter);
  }

  /* ── System Eyebrow ─────────────────────────── */
  .system-eyebrow {
    font-family: var(--font-sans);
    font-size: var(--text-micro);
    letter-spacing: var(--track-caps);
    color: var(--amber-mid);
    font-weight: 500;
    text-transform: uppercase;
    margin: 0 0 24px;
    text-align: center;
  }

  /* ── Shared section ─────────────────────────── */
  .section {
    padding: 48px 0;
    background: var(--cream);
  }

  .section--navy   { background: var(--navy-deep); }
  .section--cream  { background: var(--cream-deep); }
  .section--pt0    { padding-top: 0; }

  .s-eyebrow {
    font-family: var(--font-sans);
    font-size: var(--text-micro);
    letter-spacing: var(--track-caps);
    color: var(--amber-mid);
    font-weight: 500;
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .s-eyebrow--light { color: var(--amber-mid); }

  .section-h2 {
    font-size: var(--text-h2);
    font-weight: 500;
    color: var(--navy);
    margin: 0 0 24px;
    line-height: 1.2;
    letter-spacing: var(--track-tight);
  }

  .section-h2--light { color: var(--blue-text); }

  /* ── System Section ─────────────────────────── */
  .system-section {
    background: var(--navy);
    color: var(--blue-text);
    padding: 48px 0 104px;
  }

  .system-head {
    max-width: var(--measure-body);
    margin: 0 auto 36px;
  }

  .system-title {
    font-size: var(--text-h1);
    font-weight: 500;
    color: #fff;
    margin-bottom: 14px;
    line-height: 1.14;
    letter-spacing: -0.02em;
  }

  .system-title em {
    font-style: italic;
    color: var(--amber-mid);
    font-weight: 500;
  }

  .system-description {
    font-size: var(--text-small);
    font-weight: 400;
    color: var(--blue-soft);
    line-height: 1.6;
    max-width: var(--measure-lede);
  }

  .system-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 56px;
    align-items: stretch;
  }

  .stages {
    display: grid;
    gap: 14px;
    height: 100%;
  }

  .stage {
    background: rgba(255,255,255,0.03);
    border: 0.5px solid rgba(255,255,255,0.08);
    border-radius: 8px;
    padding: 22px 24px;
    display: grid;
    grid-template-columns: 76px 1fr;
    gap: 22px;
    align-items: start;
  }

  .stage-label {
    font-family: var(--font-mono);
    font-size: var(--text-caption);
    font-weight: 500;
    color: var(--amber-mid);
    letter-spacing: var(--track-caps);
    text-transform: uppercase;
    line-height: 1.35;
    padding-top: 4px;
  }

  .stage-title {
    font-size: var(--text-h3);
    font-weight: 500;
    color: #fff;
    margin-bottom: 8px;
    line-height: 1.25;
    letter-spacing: var(--track-tight);
  }

  .stage-body {
    font-size: var(--text-caption);
    font-weight: 400;
    color: var(--blue-soft);
    line-height: 1.55;
  }

  /* ── Snapshot Card ──────────────────────────── */
  .snap-card {
    background: var(--navy-deep);
    border: 0.5px solid rgba(255,255,255,0.08);
    border-radius: 8px;
    padding: 22px;
    font-family: var(--font-mono);
    font-size: var(--text-caption);
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .snap-hdr {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 12px;
    border-bottom: 0.5px solid rgba(255,255,255,0.08);
    margin-bottom: 14px;
  }

  .snap-title {
    color: #fff;
    font-size: var(--text-small);
    font-weight: 500;
    font-family: var(--font-sans);
    line-height: 1;
  }

  .snap-score {
    font-family: var(--font-mono);
    font-size: var(--text-h2);
    font-weight: 500;
    color: var(--amber-pale);
    line-height: 1;
  }

  .snap-score em {
    font-style: normal;
    font-size: var(--text-body);
    color: var(--blue-mid);
  }

  .snap-row {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 10px;
    padding: 9px 0;
    border-bottom: 0.5px solid rgba(255,255,255,0.06);
    color: var(--blue-soft);
    line-height: 1.5;
  }

  .snap-row:last-child {
    border-bottom: none;
  }

  .snap-value {
    font-weight: 500;
  }

  .snap-value--cited {
    color: var(--amber-pale);
  }

  .snap-value--not {
    color: var(--danger);
  }

  .snap-legend {
    display: flex;
    gap: 14px;
    margin-top: 14px;
    padding-top: 12px;
    border-top: 0.5px solid rgba(255,255,255,0.08);
    font-size: var(--text-small);
    font-weight: 400;
    color: var(--blue-mid);
    letter-spacing: var(--track-0);
  }

  .legend-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    display: inline-block;
    margin-right: 5px;
    vertical-align: middle;
  }

  .legend-dot--cited {
    background: var(--amber-pale);
  }

  .legend-dot--not {
    background: var(--danger);
  }

  /* ── Pricing ────────────────────────────────── */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    margin-bottom: 14px;
  }

  .price-card {
    background: #fff;
    border: 0.5px solid rgba(0,0,0,0.1);
    border-radius: 8px;
    padding: 18px;
    display: flex;
    flex-direction: column;
  }

  .price-card--featured { border: 2px solid var(--amber); }

  .price-tag {
    font-size: var(--text-micro);
    font-weight: 500;
    color: var(--amber);
    letter-spacing: var(--track-caps);
    text-transform: uppercase;
    margin-bottom: 4px;
  }

  .price-tier {
    font-size: var(--text-small);
    font-weight: 500;
    color: var(--navy);
    margin-bottom: 4px;
  }

  .price {
    font-size: var(--text-h2);
    font-weight: 500;
    color: var(--navy);
    line-height: 1.1;
    letter-spacing: var(--track-tight);
  }

  .price-mo {
    font-size: var(--text-body);
    font-weight: 400;
    color: var(--muted);
  }

  .price-note {
    font-size: var(--text-small);
    color: var(--mute-soft);
    margin-bottom: 14px;
    margin-top: 2px;
  }

  .price-items { list-style: none; flex: 1; margin-bottom: 14px; }

  .price-item {
    font-size: var(--text-body);
    color: var(--ink);
    padding: 3px 0;
    display: flex;
    gap: 6px;
    line-height: 1.4;
  }

  .price-cta {
    display: block;
    text-align: center;
    padding: 12px 20px;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 5px;
    font-size: var(--text-body);
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .price-cta--primary { background: var(--amber-deep); color: var(--amber-light); }
  .price-cta--primary:hover { background: #6B3F09; }

  .price-cta--outline {
    border: 0.5px solid rgba(4,44,83,0.22);
    color: var(--navy);
  }

  .price-cta--outline:hover { background: var(--navy); color: #fff; }

  .pricing-foot {
    font-size: var(--text-body);
    color: var(--muted);
    line-height: 1.6;
  }

  /* ── CTA ────────────────────────────────────── */
  .section:has(.cta-box) {
    padding: 48px 40px;
  }

  .cta-box {
    background: var(--navy);
    padding: 40px 32px;
    text-align: center;
    color: var(--blue-text);
    border-radius: 8px;
    border: 1px solid rgba(255,255,255,0.15);
  }

  .cta-eyebrow { color: var(--amber-mid); margin-bottom: 10px; }

  .cta-title {
    font-size: var(--text-h2);
    font-weight: 500;
    color: #fff;
    margin: 0 auto 10px;
    line-height: 1.2;
    letter-spacing: var(--track-tight);
    max-width: var(--measure-tight);
  }

  .cta-body {
    font-size: var(--text-small);
    line-height: 1.6;
    color: var(--blue-soft);
    max-width: var(--measure-tight);
    margin: 0 auto 20px;
  }

  .cta-buttons {
    display: flex;
    gap: 10px;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ── Buttons ────────────────────────────────── */
  .btn-primary {
    background: var(--amber-deep);
    color: var(--amber-light);
    padding: 12px 20px;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 5px;
    font-size: var(--text-body);
    font-weight: 500;
    text-decoration: none;
    display: inline-block;
    transition: background 0.2s ease;
  }

  .btn-primary:hover { background: #6B3F09; }

  .btn-ghost {
    background: transparent;
    color: var(--blue-text);
    padding: 12px 20px;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 5px;
    font-size: var(--text-body);
    font-weight: 500;
    border: 0.5px solid rgba(255,255,255,0.3);
    text-decoration: none;
    display: inline-block;
    transition: border-color 0.2s ease;
  }

  .btn-ghost:hover { border-color: rgba(255,255,255,0.6); }

  .btn-outline {
    background: transparent;
    color: var(--blue-text);
    padding: 12px 20px;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 5px;
    font-size: var(--text-body);
    font-weight: 500;
    border: 0.5px solid rgba(255,255,255,0.3);
    text-decoration: none;
    display: inline-block;
    transition: border-color 0.2s ease;
  }

  .btn-outline:hover { border-color: rgba(255,255,255,0.6); }

  /* ── Responsive ─────────────────────────────── */
  @media (max-width: 900px) {
    .wrap-inner { padding: 0 var(--gutter); }
    .page-hero  { padding: 40px 24px 36px; }
    .page-h1    { font-size: var(--text-h1); }
    .section    { padding: 36px 0; }
    .system-section { padding: 64px 0 80px; }

    .system-title { font-size: var(--text-h2); }

    .system-grid {
      grid-template-columns: 1fr;
      align-items: start;
    }

    .snap-card { height: auto; }
  }

  @media (max-width: 640px) {
    .page-h1 { font-size: var(--text-h2); }
    .cta-title { font-size: var(--text-h3); }
  }

  /* ── Value model section ────────────────────── */
  .vv-eyebrow { color: var(--amber-ink); }

  .vv-intro {
    font-size: var(--text-small);
    line-height: 1.7;
    color: #444441;
    max-width: var(--measure-body);
    margin: 0 0 20px;
  }

  .vv-link {
    color: var(--navy);
    font-weight: 500;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .vv-link:hover { color: var(--navy-soft); }

  @media (prefers-reduced-motion: reduce) {
    .btn-primary, .btn-ghost, .btn-outline, .price-cta, .case-link, .cv-visit {
      transition-duration: 0.01ms;
    }
  }
</style>
